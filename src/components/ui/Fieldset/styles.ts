import styled from 'styled-components';

export const FieldsetContainer = styled.fieldset`
    border: none;
    padding: 10px;
    margin: 10px 0;

    legend {
        text-align: left;
        color: #141414;
        font-size: 20px;
        font-weight: bold;

        margin-left: -10px;
    }
`;
