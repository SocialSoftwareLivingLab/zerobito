import styled from 'styled-components';

export const Container = styled.div`
    form > div {
        margin-left: 0;
        margin-right: 0;
    }

    footer {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: space-between;
    }
`;
