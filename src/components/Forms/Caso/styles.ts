import styled from 'styled-components';

export const FormContainer = styled.form`
    & > div {
        margin: 8px 0;
    }

    div.submit {
        display: flex;
        flex-direction: row;
        justify-content: flex-end;
    }
`;
